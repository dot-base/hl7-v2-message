{{#each (values versionTypes.messages)}}
{{> versionIndexTypeImport name=name type="Message"}}
{{/each}}

{{#each (values versionTypes.segments)}}
{{> versionIndexTypeImport name=type type="Segment"}}
{{> versionIndexTypeImport name=type type="Fields"}}
{{/each}}

import { getMessage, getSegment } from "./utils"
import parse from "./parser/hl7Parser"

export default {
    messages: { {{#each versionTypes.messages}}{{> versionIndexTypeName name=name type="Message"}}{{/each}} },
    segments: { {{#each versionTypes.segments}}{{> versionIndexTypeName name=type type="Segment"}}{{/each}} },
    fields: { {{#each versionTypes.segments}}{{> versionIndexTypeName name=type type="Fields"}}{{/each}} },
    utils: { getMessage, getSegment },
    parser: { parse }
};