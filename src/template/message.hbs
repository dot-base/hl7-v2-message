import { Hl7Message } from '../../../model/hl7Message';
import Hl7IMessageDefinition from '../../../types/hl7IMessageDefinition';
import hl7ICompoundDefinition from '../../../types/hl7ICompoundDefinition';

{{#each segments}}
{{> messageSegmentImport}}
{{/each}}

/**
* {{message.description}}
*/
export class {{message.name}}_Message extends Hl7Message {
    public name = '{{message.name}}';
    public description = '{{message.description}}';
    public segments = {
        {{#each segments}}
        {{> messageSegment}}
        {{/each}}
    };
}