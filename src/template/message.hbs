import Hl7Message from '../../../model/Hl7Message';
import Hl7IMessageSegment from '../../../types/Hl7IMessageSegment';
import Hl7IMessageCompound from '../../../types/Hl7IMessageCompound';

{{#each (unique (values segments))}}
{{> messageSegmentImport}}
{{/each}}

/**
* {{description}}
*/
export default class {{name}}_Message extends Hl7Message {
    public get name(): string { return '{{name}}'; }
    public get description(): string { return '{{description}}'; }
    public compounds = {
        {{#each (values segments)}}
        {{> messageCompound}}
        {{/each}}
    };
    public segments = {
        {{#each (values segments)}}
        {{> messageSegment}}
        {{/each}}
    };
}