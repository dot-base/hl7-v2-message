/**
* {{description}}
*/
public {{identifier}}: Hl7Field = {
	index: {{index}},
	identifier: "{{identifier}}",
	description: "{{description}}",
	length: {{#if (isDefined length)}}{{length}},{{else}}undefined,{{/if}}
	isOptional: {{isOptional}},
	isRepeatable: {{isRepeatable}},
	value: "{{value}}",
	{{#if (isDefined subFields)}}
	subFields: {{subFields}}
	{{/if}}
};
