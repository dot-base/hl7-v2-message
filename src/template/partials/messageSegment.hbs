{{#if isSegment}}
public {{type}}: Hl7IMessageDefinition<{{type}}_Segment> = {
    type: "{{type}}",
    isOptional: {{isOptional}},
    isRepeatable: {{repeatable}},
    {{#if compoundDefinition}}
    {{!-- TODO: rename to parent / group? --}}
    compoundDefinition: {{compoundDefinition}},
    {{/if}}
    value: []
};
{{else}}
public {{type}}: hl7ICompoundDefinition = {
    type: "{{type}}",
    isOptional: {{isOptional}},
    isRepeatable: {{repeatable}},
    {{#if compoundDefinition}}
    compoundDefinition: {{compoundDefinition}},
    {{/if}}
};
{{/if}}