{{#if parentCompound}}{{> messageSegmentNamePrefix segment=parentCompound}}{{/if}}{{type}}: {
    type: "{{type}}",
    isOptional: {{isOptional}},
    isRepeatable: {{isRepeatable}},
    {{#if parentCompound}}
    parentCompound: this.segments.{{parentCompound.type}},
	{{/if}}
	{{#if (isDefined value)}}
	value: []
	{{/if}}
} as {{#if (isDefined value)}}Hl7IMessageSegment<{{type}}_Segment>{{else}}Hl7IMessageCompound{{/if}},
