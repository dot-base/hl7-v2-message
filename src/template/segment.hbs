import { Hl7Segment } from "../../../model/hl7Segment";
import { {{type}}_Fields } from '../fields/{{toLowerCase type}}_Fields';

/**
* {{description}}
*/
export class {{type}}_Segment extends Hl7Segment {
    public type = "{{type}}"; 
    public description = "{{description}}"; 
    {{!-- TODO: rename to fields or strip it like in message --}}
    public children: {{type}}_Fields = new {{type}}_Fields(); 
}
